# === Database ===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=docparser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=docparser
DATABASE_URL=postgresql+asyncpg://docparser:changeme@postgres:5432/docparser

# === Redis ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://redis:6379
REDIS_QUEUE_NAME=arq:queue

# === MinIO ===
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_RAW=raw-files
MINIO_BUCKET_RESULTS=parse-results
MINIO_BUCKET_IMAGES=parse-images

# === API ===
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true
API_WORKERS=4
CORS_ORIGINS=*

# === Worker ===
WORKER_CONCURRENCY=2
WORKER_QUEUE=arq:queue

# === GPU ===
GPU_DEVICE=cuda
GPU_MODEL_BASE=/models
PYTORCH_INFER_URL=http://gpu-pytorch:8001
PADDLE_INFER_URL=http://gpu-paddle:8002

# === Tools ===
LIBREOFFICE_HOST=tools
LIBREOFFICE_PORT=8100
PANDOC_HOST=tools
PANDOC_PORT=8200

# === Storage ===
UPLOAD_DIR=/data/uploads
RESULT_DIR=/data/results
TEMP_DIR=/tmp
MODEL_CACHE_DIR=/models/cache

# === Auth ===
SECRET_KEY=change-this-to-a-random-secret-key
API_KEY=change-this-to-a-random-secret-key
JWT_EXPIRATION_SECONDS=86400

# === Frontend ===
VITE_API_BASE_URL=http://localhost:8000
